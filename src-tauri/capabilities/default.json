{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the app (main window only).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
